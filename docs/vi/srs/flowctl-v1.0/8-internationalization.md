## 8. Internationalization

### 8.1 UI và CLI locale

- Dashboard: chuỗi UI nhúng trong Python — **TBD** có hỗ trợ đa ngôn ngữ hay không (wiki không nêu i18n dashboard).

### 8.2 Thông báo lỗi / merge MCP

- Wiki `merge_cursor_mcp.py`: thông báo lỗi **tiếng Việt** trên stderr khi JSON không hợp lệ (nhánh non-overwrite).

### 8.3 Yêu cầu locale khác

**TBD** — không có danh sách locale sản phẩm trong wiki.
