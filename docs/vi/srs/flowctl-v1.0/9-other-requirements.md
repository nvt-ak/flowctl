## 9. Other Requirements

### 9.1 Packaging / npm

Wiki **overview** đề cập script `package.json`: `npm run setup-hooks`, `npm run flowctl`, `test:*`, `test:gate:local`, `test:ci`, `release:*`, `prepare` — chi tiết semver: **TBD — đọc `package.json` nếu cần đưa vào hợp đồng phát hành**.

### 9.2 Lệnh bổ sung engine

- `cmd_retro` được nhắc trong module tree (wiki orchestration liệt kê `retro.sh`) — hành vi chi tiết: **TBD — wiki trang orchestration chính không mở rộng retro trong phần đã trích**.

### 9.3 Pháp lý / license phân phối

**TBD** — ngoài phạm vi wiki GitNexus.

### 9.4 Đồng bộ tài liệu repo

Wiki **documentation.md** yêu cầu đọc `README.md`, `CLAUDE.md`, `AGENTS.md`, `docs/` — đây là governance human-facing, không phải requirement thực thi runtime; giữ là **tham chiếu vận hành** REF-WIKI-07.
