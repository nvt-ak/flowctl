# Performance Standards — flowctl

**SRS Reference:** Section 7.1

---

## 1. Latency targets

**TBD** cho CLI subcommands — wiki không đưa SLO ms.

## 2. Caching

- MCP strategies: static/git/state/ttl/mtime (wiki).
- Monitor watcher 200ms poll; SSE ping 25s.

## 3. Throughput

**TBD** — max events/s trên `events.jsonl`.

## 4. DB

N/A SQL — xem file I/O size guidance: **TBD**.
