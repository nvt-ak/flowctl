---
description: Start a step — open INDEX + minimal flowctl commands (do not run bash blindly)
---

Read [.cursor/INDEX.md](.cursor/INDEX.md) and [CLAUDE.md](CLAUDE.md).

In the project, run:

```bash
flowctl status
flowctl assess
flowctl start
```

Then follow the step: `flowctl complexity` → `flowctl cursor-dispatch` (see `docs/workflow-reference.md` for War Room flags and details).
