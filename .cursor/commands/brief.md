---
description: Reminder — worker brief path for the current step
---

Briefs live under `workflows/dispatch/step-<N>/` — file `<role>-brief.md`. Replace `<N>` with `current_step` from `flowctl status` or `wf_state()`.

War Room (if used): `workflows/dispatch/step-<N>/war-room/*-brief.md`.
